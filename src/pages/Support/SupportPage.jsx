import React from 'react';
import SupportSection from '../../screens/SupportSection';

/**
 * Support page component that uses the existing SupportSection component
 */
const SupportPage = () => {
  return (
    <SupportSection />
  );
};

export default SupportPage;
